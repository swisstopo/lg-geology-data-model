
# Datenmodell Geologie, Version 3.5 #


![Extrait GeoCover vers le Moléson](geocover.png "Moléson")





\pagebreak


{% for theme in themes -%}
{% set themeloop = loop %}


## Theme {{ theme.name |upper}} ##

{% for cls in theme.classes -%}



### Class {{ cls.name }} ### {{ '{#'}}{{cls.name | slugify}}{{'}'}}
{{ cls.description| trim|highlight}}




    | Name                         | Type                              | Description
----|------------------------------|-----------------------------------|--------------------------------------
{% for attr in cls.attributes -%}
{% set slug = "%s-%s" % (cls.name, attr.name) %}
{%- if attr.att_type == 'CD' -%}
{{loop.index}} | **{{ attr.name  }}**                | [CodedDomain](#{{slug| slugify}})                    | {{ attr.description|trim }} 
[]()           | Cardinality [{{ attr.cardinality}}] |                                                      |  
{% else -%}
{{loop.index}} | **{{ attr.name  }}**                | {{attr.att_type}}                                    | {{ attr.description|trim }} 
[]()           | Cardinality [{{ attr.cardinality}}] | {{attr.value}}                                       |  
{% endif -%}
                       
{% endfor %}



{% for attr in cls.attributes -%}
{% set slug = "%s-%s" % (cls.name, attr.name) %}
#### Attribute {{ attr.name }}
{%- if attr.att_type == 'CD' -%}{{ '{#'}}{{slug| slugify}}{{'}'}}{% endif %}
_{{ attr.description|trim|highlight}}_
{%- if attr.att_type in ('boolean', 'string', 'float', 'range', 'integer') %}
_Datentyp:  {{ attr.att_type  }}_
{% endif %}

{% if 'pairs' in attr -%}
|GeolCode|Deutsch|Français|
|---------------|----------------------------------------|----------------------------------------|
{% for key, value in attr.pairs.items() -%}
|{{key}} | {{value}} | {{value}}     |
{% endfor %}
{% endif %}
   
{% endfor %}
{% endfor %}
{% endfor %}
